#pragma once
#include <string>
#include <vector>

typedef std::string str;

void read_inputs(std::vector<str>&);

void read_input(str&, str&, str&, str&,
		int&, int&, double&, double&, double&, double&, double&);
